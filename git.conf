repo.uri=
repo.branch=
ignition.project.name=

ignition.user.name=
user.name=
user.email=

commissioning.import.themes=TRUE
commissioning.import.tags=TRUE
commissioning.import.images=TRUE

# 0 or omitted = auto-pull disabled. When > 0, pulls this project on that
# interval using the ignition.user.name credentials configured above.
commissioning.auto.pull.interval.minutes=0
